{
  "name": "camera-cutout-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch --env-file=.env src/server.ts",
    "start": "node --env-file=.env dist/src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/multipart": "^9.3.0",
    "fastify": "^5.8.5",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@11.9.0"
}
