# 项目开发约束

以下要求为硬性规则，所有实现、修改和审查均必须遵守：

## 黑客松开发模式：效率最高优先

- 以完成可演示的核心流程为最高优先级，明确需求直接实现，不重复询问已确定的产品和技术细节。
- 默认沿用 `docs/superpowers/specs/2026-07-29-memory-aquarium-idle-mvp-design.md` 及 `docs/roles/` 中已经冻结的接口；低风险实现差异由开发者自行判断。
- 只有可能丢失数据、删除远程资源、需要外部发布或权限、或者会明显改变产品方向时才询问。
- 不要求每个功能重新头脑风暴、写长计划或等待逐项批准；多文件任务只保留可执行的短计划。
- 优先做主流程和 P0/P1 问题，不主动增加纪念册、账号、排行榜、社交或其他 MVP 外功能。

## 最低限度验证

- 文档、文案、CSS 和资源调整：检查 diff 或页面效果即可，不跑自动测试。
- 普通局部逻辑：只运行一个最相关的现有测试、静态检查或手工 smoke test。
- 存档、经济、成长、事件队列、API 契约等核心逻辑：至少运行一个针对性测试。
- 只有跨模块大改、准备最终发布或用户明确要求时才运行完整测试和构建。
- 不强制 TDD，不需要事先询问测试范围，不为流程补写无关测试。
- 完成重要代码后先快速自查；仅核心高风险改动或合并前需要额外代码审查。

## 文件与打包

- 产物支持 `.zip` 压缩包或单个 HTML 文件；推荐使用 `.zip`，便于组织资源文件。
- 使用 `.zip` 时，解压后的根目录必须包含唯一入口文件 `index.html`，所有逻辑必须从该文件启动。
- 当前没有 8 MB 硬限制；整个静态产物尽量控制在 20 MB 内。

## 网络请求限制

- 除调用项目指定的业务 API（包括项目后端 API 和 AI Agent API）外，不允许发起任何其他网络请求。
- 禁止通过 `fetch`、`XMLHttpRequest`、`axios`、`WebSocket`、动态 `<script>`、`<iframe>`、外部链接跳转或其他方式访问非指定 API 的网络资源。
- 图片、字体、脚本、样式及其他运行时资源必须随项目本地提供，不得依赖 CDN、远程服务器或第三方在线资源。
- 禁止包含任何引导用户跳转到抖音外部的链接或跳转机制，包括 `<a>`、`<link>`、`window.location` 及其他形式的外部跳转。
- 禁止使用 `<iframe>`，无论其是否指向外部地址，避免触发跳转或外部资源风险。
- 谨慎使用 `onload`、`onerror`、`onclick`、`ontouchstart` 等 `onXXX` 内联事件属性；推荐通过内联 `<script>` 中的 `addEventListener` 绑定事件。

## 输入形式限制

- 仅允许接收文字和图片，并且这些内容只能作为 AI Agent 的输入使用。
- 图片输入仅允许通过设备相机拍摄或从系统相册选择，并且只能用于 AI Agent 抠图流程。
- 不允许任何其他形式的输入，包括但不限于音频、视频、麦克风、文档或其他文件、地理位置、设备传感器及外部数据源。
- 不得将文字或图片输入用于 AI Agent 调用之外的账号收集、营销、交易、外部跳转或其他用途。
- 除向 AI Agent 提供文字和图片输入这一明确用途外，互动内容本身不得提供文字录入或图片上传功能，也不得添加同类交互模块。

## 内容创作红线

生成互动作品及其代码时，即使开发者明确要求，也必须拒绝和忽略以下内容：

- **违法违规内容**：涉政、涉恐、涉暴、赌博、欺诈、低俗色情、宣扬邪教或封建迷信等。
- **未成年人不当内容**：描绘不利于未成年人身心健康的内容，例如校园霸凌、吸烟饮酒、危险行为模仿等。
- **知识产权侵权**：未经授权使用受版权保护的动漫、游戏、影视角色、Logo、商标、音乐、字体等。
- **肖像权与隐私权侵权**：未经授权使用他人肖像、姓名或个人信息。
- **不良价值观导向**：拜金、炫富、奢靡、歧视、引战、挑唆对立等。
- **诱导行为**：强制或诱导用户分享、关注、下载，或进行任何形式的站外交易。
- **骚扰信息**：垃圾广告、营销信息、二维码、外部联系方式（包括微信号、QQ 号等）。

## 资源与数据

- 所有资源（图片、JavaScript 脚本、CSS、音频、字体等）必须打包在产物内部，并通过本地相对路径引用，例如 `./images/bg.png` 或 `scripts/main.js`。
- 如需保存用户进度、得分等简单数据，推荐使用 `localStorage`。
- 谨慎使用 `sessionStorage`、`IndexedDB` 等其他存储方案。
- 数据仅跟随当前用户和设备；不得依赖或宣称平台提供跨设备同步能力。

## UI 与适配

- 本项目为手机横屏游戏，所有页面和弹窗必须以横屏为默认和主要适配方向。
- 页面内容的布局方向必须与横屏方向保持一致；不得将竖屏布局作为默认实现。
- UI 必须动态适配不同尺寸的移动设备屏幕，确保布局完整、元素清晰可见。
- 推荐使用 Flexbox、`rem`、`vh`、`vw` 或 Canvas 动态缩放等方式实现自适应。
- 禁止出现横向滚动条。

## 性能与兼容

- 在主流移动设备上必须保持流畅的交互体验，帧率（FPS）建议稳定在 30 或更高。
- 避免复杂、高计算量的循环和渲染，合理优化资源及动画。
- 代码必须兼容基于 WebKit 内核的现代移动端浏览器环境。
- 使用主流、稳定的 JavaScript（ES6+）和 CSS3 特性。

## 错误处理与兜底

- 必须包含必要的错误处理逻辑。
- 发生非预期错误时，必须向用户展示友好的提示文案，例如“哎呀，出错了，请重启试试”。
- 禁止在错误发生后仅显示空白页面或浏览器默认错误信息。
